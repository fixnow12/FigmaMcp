# Bridge guide-fidelity v2 — кандидат

Статус: реализация и локальные проверки завершены; живая приёмка и независимый review выполняются в родительской задаче GuideConstructor. Коммиты, push, публикации и переключение рабочего runtime здесь не выполнялись.

Ветка `codex/guide-fidelity-bridge`, plugin manifest `0.3.0+codex.20260919-fidelity-v2`. Конечные ревизии: MCP `15e1ce93a4fb9a0d`, broker `0928fb75dc78eb15`, plugin `c8e5c8716961ba78`. Код/UI generic Figma plugin не менялись. Изолированная сборка: `.tmp-figma/guide-fidelity-v2/build/figma-plugin/manifest.json`; она имеет отдельную identity и не должна заменять текущую рабочую установку без отдельного подключения. Фактически созданный code.js проверен `node --check`.

Реализованы списки и отступы в тексте/диапазонах, GLASS, применимые нативные векторные свойства и формы, диапазонные variable bindings с загрузкой шрифтов и восстановлением aliases, исправление NAVIGATE resetScrollPosition, полный read-back новых полей и независимое сравнение. Геометрия в экземплярах доступна только как точный no-op; реальные изменения явно блокируются. Линейные VECTOR/LINE допускают height:0; strokeCap/strokeJoin ограничены этими типами. Новые native booleanOperation сохраняют обоих потомков в нормализации.

GLASS сверён с https://raw.githubusercontent.com/figma/plugin-typings/master/plugin-api.d.ts; `splay` дополнительно подтверждён живым source read родителя (node3397:9726), но сам write/readback ещё требует живой проверки. Для NAVIGATE подтверждённый source read `resetScrollPosition:true` нормализуется только как default; явное false, неизвестные поля и противоречивые параметры не скрываются. Клики в Present не проверялись этой частью работы.

Контракт конструктора: свежий get_status.diagnostics, ready=true, current.mcp/broker/plugin=true, capabilityFileKey равен fileKey; непустые string expectedPluginBuild и mcp.revision; capabilities содержит нужные поля. Возможности не объявляются без адресного responsive idle execution probe. Constructor agent проверил все1123 команды по всем14 комбинациям через актуальные Zod schemas: ошибок0 (артефакт в GuideConstructor/runs/fidelity-v2-constructor/bridge-schema-validation.json).

Локальные проверки:

- `npm --prefix plugins/figma-local-bridge test`: 410 passed, 0 failed.
- `node plugins/figma-local-bridge/scripts/export-schema.mjs --check`: passed.
- `node plugins/figma-local-bridge/scripts/verify-install.mjs`: новый MCP-процесс публикует и подтверждает расширенные схемы.
- `node --check .tmp-figma/guide-fidelity-v2/build/figma-plugin/code.js`: passed.
- `git diff --check`: passed.

Логи и TDD evidence: `.tmp-figma/guide-fidelity-v2/`. Первые новые проверки завершались ожидаемыми ошибками отсутствующего контракта. Найденные в процессе дефекты размеров векторов, утраты boolean operands и append preflight воспроизведены регрессиями и исправлены до зелёного полного suite. Полные локальные проверки не означают завершения парной live-матрицы.

Подключение кандидата: новый stdio MCP через `node <FIGMA_MCP_ROOT>/plugins/figma-local-bridge/src/server.mjs`, cwd `<FIGMA_MCP_ROOT>/plugins/figma-local-bridge`, `FIGMA_WS_HOST=127.0.0.1`, прежний `FIGMA_LOCAL_STATE_DIR` (по умолчанию `~/.figma-local-bridge`). После завершения старых операций штатный broker согласует актуальную ревизию. Проверить адресный get_status целевого файла и его read-back перед записью. Старый каталог текущего чата не получает новые start/end автоматически; используйте новый MCP Client/переподключение.

После live-проверки обложки и независимого review добавлены scoped corrections: captured layout extras при итоговом NONE допустимы только как точный no-op по текущим native значениям; изменение неактивной раскладки остаётся not_applied. При откате равные structural и layout свойства повторно не назначаются, чтобы запрещённый setter внутри INSTANCE либо NONE не создавал ложный partial. Все три случая воспроизведены red→green. Font preflight сохранён: родитель подтвердил доступность Menlo в Desktop Bridge, а исходные шрифты нужны для надёжного отката.

Вторая live-проба resource-overrides логотипа выявила readonly inherited instance properties: isMask/maskType/x/y/rotation/layoutPositioning/constraints. Реальные отличия этих свойств у descendants INSTANCE теперь блокируются на полном preflight; exact no-op не вызывает setter. Корневой INSTANCE остаётся допустимым target обычных overrides. Откат пропускает все равные scalar/compound значения и неизменённые размеры, поэтому не создаёт ложного partial запрещёнными setters. Три дополнительные regression red→green, общий suite394/394. Никакого detach или замены экземпляров.

Финальное exact no-op сравнение структурное: порядок ключей объекта незначим, но состав ключей, значения, длина и порядок массивов сравниваются полностью. Числового допуска нет. Это согласовано с official adapter и покрыто red→green регрессией constraints key order. Финальный suite395/395; sourceRevision26990d836e1403a8.

Live-проба scaleFactor выявила отличие native INSTANCE scale от width/height resize: source3397:10007 имеет scaleFactor1.0373859405517578. Добавлены patch_nodes.scaleFactor, full inspect и capability instance-scale-v1. Положительный конечный абсолютный масштаб применяется через native rescale(target/current) перед resize; отношение ограничено 0.01–100 для обратного API-вызова. Заранее загружаются все subtree TEXT fonts, включая скрытые; meaningful nested scaling и пересекающиеся ancestor/descendant patches в том же пакете блокируются. Rollback возвращает исходный масштаб, затем размеры. В тестах подтверждён actual source factor, отказ native API после частичной мутации, последующая ошибка другого patch и недоступный скрытый шрифт. Также min/max потомков INSTANCE принимаются только как точный no-op. Полный итог403/403; schema, новый MCP-каталог, build syntax и diff-check прошли. Runtime не переключался.

Live-проба arrow (Bridge request25/response24) выявила единственную входную разницу: native fills возвращает boundVariables:{}, а command опускает поле. Все scalar/geometry были точны. Повторное назначение заливки может пересчитать BOOLEAN_OPERATION bounds и привести к запретному setter x внутри INSTANCE. Paint no-op теперь приравнивает только пустой map boundVariables к отсутствию на paint/gradient stop; непустые aliases, прочие поля и числа сохраняют строгое сравнение. При rollback неизменённые style ids повторно не применяются. Side-effect regression воспроизводит сдвиг и readonly ошибку; RED3→GREEN33 targeted, полный suite407/407. Frozen source85cebe906cc51ca4; broker/plugin unchanged; schema и MCP verifier прошли. Живое восстановление собственного failedarrow выполняет родитель.

Итоговое сравнение пяти сцен обнаружило потерю dormant layout metadata у common-grid: ранний return в applyLayout после NONE пропускал gap/padding/alignment. Теперь эти разрешённые native свойства сохраняются без включения Auto Layout; wrap/counterAxisSpacing/strokesIncludedInLayout/itemReverseZIndex на NONE остаются exact no-op-only. Три регрессии RED→GREEN подтверждают render, patch с сохранением child geometry и rollback после later failure. Полный suite410/410, schema/MCP verifier/diffcheck pass, frozen source15e1ce93a4fb9a0d. Отдельная потеря FILL у пяти zero-height LINE вызвана adapterguard; constructor agent исправляет адаптер. Точные шесть repair patches по canonical scene/current IDs записаны в .tmp-figma/guide-fidelity-v2/layout-repair-args.json; provenance в layout-repair-evidence.json. Исполнение и live readback — родитель.
