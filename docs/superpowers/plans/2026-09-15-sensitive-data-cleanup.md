# Очистка чувствительных данных — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. Независимую проверку готового diff делегировать read-only агенту; процесс соразмерять AGENTS.md.

**Goal:** Обезличить опубликованные идентификаторы, предотвратить их повторное добавление и подготовить отдельное решение об очистке истории Git.

**Architecture:** Сначала изменить документацию и расширить существующий валидатор репозитория. Корпоративный email заменить в локальной конфигурации только этого checkout. Историю обрабатывать отдельным этапом в изолированном клоне, без изменения рабочего проекта до согласования точного результата.

**Tech Stack:** Git, Node.js >=20, node:test, Gitleaks 8.24.3 или проверенная актуальная версия, GitHub Actions; git-filter-repo только для согласованного этапа истории.

**Spec:** Запрос пользователя о чувствительных данных и следующий запрос составить план; конкретный объём закреплён в разделе [«Исходные данные и границы»](#исходные-данные-и-границы).

## Исходные данные и границы

План подготовлен для `fixnow12/FigmaMcp`, исходная ревизия `cba085570036c54766227f242c78125f9f1b1640`. На момент проверки: публичный GitHub-репозиторий, 137 tracked файлов, 46 доступных коммитов, чистое рабочее дерево.

Обнаружено:

- Один реальный идентификатор Figma в `docs/figma-root-sizing-fix.md:43` и `docs/figma-workflow-latency-2026-09-12.md:46`.
- Три ID сессий OpenCode в `docs/opencode-iomoney-project-fix-2026-09-11.md:4` и `docs/opencode-target-status-fix-2026-09-10.md:3,21`.
- Корпоративный email автора или коммитера в 45 из 46 коммитов; текущая Git-конфигурация наследует его из глобального файла.
- Gitleaks: 0 находок в текущем tracked дереве; 9 срабатываний в полной доступной истории. Восемь относятся к Figma fileKey, одно — к литералу старого HMAC-теста. Это не девять подтверждённых рабочих секретов.
- Игнорируемый каталог `.tmp-figma/`: 35 файлов с диагностикой и содержимым макетов. Ярлык `Плагин Figma` ведёт во внешнюю персональную сборку, содержащую приватный seed; её файлы имеют права `0600`.
- Названия рабочих проектов и библиотек в исторических документах раскрывают контекст. Сокращённое имя и маскированный телефон в опубликованном экране сопровождаются пометкой «Демо»; реальное происхождение персональных данных не установлено.

## Global Constraints

- Общение и новая документация — на русском.
- На этапе составления плана меняется только этот план. Следующие шаги выполняются после запроса на реализацию.
- Не менять runtime Bridge, криптографический протокол, публичные MCP-инструменты, название репозитория и GitHub Topics.
- Не изменять макеты Figma, библиотеки, рабочие ключи установки и персональную сборку.
- Не удалять `.tmp-figma/` и другие локальные материалы автоматически.
- Не помещать реальные ID, email или seed в план, тесты, логи и новые отчёты; отчёты сканера сохранять вне Git с маскированием значений.
- Не публиковать и не переписывать историю автоматически. Перед обычным push показать remote, ветку, состав, SHA; перед переписыванием — также карту refs и старых/новых SHA, затем получить отдельное подтверждение.
- Репозиторные изменения Git-настроек должны быть `--local`; глобальные настройки пользователя не менять.
- Названия проектов и демонстрационные данные не удалять массовой заменой. Их дополнительная редактура требует отдельного решения о содержании, поскольку это не подтверждённые credentials.

## Карта файлов

| Файл | Изменение |
| --- | --- |
| `docs/figma-root-sizing-fix.md` | Обезличить ID файла |
| `docs/figma-workflow-latency-2026-09-12.md` | Обезличить тот же ID |
| `docs/opencode-iomoney-project-fix-2026-09-11.md` | Обезличить ID сессии |
| `docs/opencode-target-status-fix-2026-09-10.md` | Обезличить два ID сессий |
| `scripts/sensitive-data.mjs` — новый | Чистая функция поиска идентификаторов без вывода их значений |
| `scripts/validate-repo.mjs` | Вызов нового детектора в существующем обходе файлов |
| `plugins/figma-local-bridge/tests/sensitive-data.test.mjs` — новый | Регрессии обнаружения, допустимых примеров и безопасного вывода |
| `docs/maintenance.md` | Правила публикации, локальных данных и Git-email |
| `.git/config` | Локальный noreply email; файл не попадает в коммит |

`.gitignore` и существующий CI уже выполняют нужную базовую роль. Менять их только если проверка выявит конкретный пробел; не добавлять дублирующий workflow.

## Задача 1. Зафиксировать исходное состояние и адрес будущих коммитов

**Files:** `.git/config` — только локальная настройка; отчёты и снимок исходных refs — вне репозитория.

- [x] Выполнить `git status --short --branch`, `git remote -v`, `git fetch --prune origin`, `git rev-list --left-right --count HEAD...origin/main`. Сохранить постороннюю работу; при изменении remote или состава начать с новой сверки.
- [x] Прочитать GitHub login и ID, получить noreply-адрес без предположений о формате старого аккаунта:

```bash
gh api user --jq '"\(.id)+\(.login)@users.noreply.github.com"'
```

- [x] Сохранить наличие и прежнее значение локального `user.email` в приватном отчёте. Использовать возвращённый noreply-адрес в `git config --local user.email`; `user.name` оставить прежним. Проверить фактические author/committer identities с учётом переменных окружения. Если окружение переопределяет email, устранить только подтверждённое переопределение в текущей операции.
- [x] Повторить masked Gitleaks отдельно для tracked snapshot и истории `--log-opts=--all`. Не считать зелёный push-CI доказательством чистоты всех старых коммитов.

**Результат:** известны актуальные base/remote/refs; будущий коммит этого checkout использует noreply; рабочие файлы пока не очищены.

## Задача 2. Обезличить пять текущих вхождений

**Files:** четыре диагностических Markdown-файла из карты выше.

- [x] Повторно прочитать указанные места; номера строк могут сдвинуться.
- [x] Оба вхождения Figma заменить на `<FILE_KEY>`, сохранив размеры, nodeId и технический смысл отчётов.
- [x] Три разных ID OpenCode заменить последовательно на `<SESSION_ID_1>`, `<SESSION_ID_2>`, `<SESSION_ID_3>`. Одинаковые исходные ID при дополнительных вхождениях должны получить одинаковую метку.
- [x] Просмотреть diff и проверить, что реальные значения не перенесены в комментарии, сообщения коммита или план. Проверить относительные ссылки и `git diff --check`.

**Результат:** текущая документация не содержит пяти подтверждённых идентификаторов. История Git на этом этапе сохраняется.

## Задача 3. Закрыть пропуски существующего валидатора

**Files:** создать `scripts/sensitive-data.mjs`, создать `plugins/figma-local-bridge/tests/sensitive-data.test.mjs`, изменить `scripts/validate-repo.mjs`.

**Interfaces:**

```ts
// Контракт scripts/sensitive-data.mjs; реализация — обычный JavaScript.
// В результате нет исходного текста, совпадения или секретного значения.
findSensitiveIdentifiers(text: string): Array<{ rule: string; line: number }>;
```

Правила узкие и объяснимые: ID сессии OpenCode; Figma-ID в URL, присваивании fileKey и рядом с русским обозначением файла. Не объявлять любой длинный идентификатор секретом: имена API вроде layoutSizingHorizontal должны проходить. Текущие правила токенов и private-key markers валидатора сохранить.

- [x] Сначала создать регрессионные тесты. Использовать синтетические данные, собираемые во время выполнения:

```js
import test from 'node:test';
import assert from 'node:assert/strict';
import { findSensitiveIdentifiers } from '../../../scripts/sensitive-data.mjs';

test('находит Figma-ID в обычном тексте и ID сессии, не раскрывая значения', () => {
  const file = 'Ab9'.repeat(7) + 'Z';
  const session = 'ses_' + 'x'.repeat(26);
  const source = 'Живая проверка в файле `' + file + '`:\nСессия ' + session;
  const result = findSensitiveIdentifiers(source);
  assert.deepEqual(result, [
    { rule: 'figma-file-id', line: 1 },
    { rule: 'opencode-session-id', line: 2 },
  ]);
  assert.equal(JSON.stringify(result).includes(file), false);
  assert.equal(JSON.stringify(result).includes(session), false);
});

test('пропускает placeholders и названия свойств API', () => {
  assert.deepEqual(findSensitiveIdentifiers(
    '<FILE_KEY> <SESSION_ID_1> layoutSizingHorizontal allowComponentChanges'
  ), []);
});
```

- [x] Добавить табличные случаи: JSON fileKey; Figma design/file/proto URL, в том числе Markdown-ссылки, autolinks и URL перед пунктуацией; «файл» и «в файле»; CRLF и несколько находок в одной строке; текст без идентификатора. Синтетические строки собирать из фрагментов, чтобы тестовый исходник сам не выглядел как опубликованный ID.
- [x] Запустить `node --test plugins/figma-local-bridge/tests/sensitive-data.test.mjs`, убедиться, что после создания пустого экспортируемого детектора тесты падают на отсутствии нужных находок, а не только на ошибке импорта.
- [x] Реализовать детектор без файловых и сетевых операций. Базовые регулярные выражения:

```js
const rules = [
  ['opencode-session-id', /\bses_[A-Za-z0-9]{15,}\b/g],
  ['figma-file-id', /https?:\/\/(?:www\.)?figma\.com\/(?:design|file|proto|board)\/[A-Za-z0-9]{20,64}(?=$|[^A-Za-z0-9_-])/g],
  ['figma-file-id', /\bfileKey["'`]?\s*(?:=|:)\s*["'`]?[A-Za-z0-9_-]{20,64}(?=["'`\s,})]|$)/gi],
  ['figma-file-id', /(?:файле|файл)\s+[`"'][A-Za-z0-9]{20,64}[`"']/gi],
];

export function findSensitiveIdentifiers(text) {
  const findings = [];
  for (const [index, line] of text.split(/\r?\n/).entries()) {
    const found = new Set();
    for (const [rule, pattern] of rules) {
      pattern.lastIndex = 0;
      if (pattern.test(line) && !found.has(rule)) {
        findings.push({ rule, line: index + 1 });
        found.add(rule);
      }
    }
  }
  return findings;
}
```

Сканировать построчно через `split(/\r?\n/)`; сбрасывать `lastIndex` каждого regex перед строкой; возвращать не более одного результата одного правила на строку. Это детектор известных форм, а не обещание распознать любой ID в произвольном контексте.

- [x] Подключить в уже существующий обход валидатора:

```js
import { findSensitiveIdentifiers } from './sensitive-data.mjs';
// В scan() после чтения content:
for (const finding of findSensitiveIdentifiers(content)) {
  findings.push(`${path}:${finding.line}: ${finding.rule}`);
}
```

- [x] Не менять текущий запрет следовать symlink и исключения `.tmp-figma`, `node_modules`, `migration-backup`, `dist`, `.git`.
- [x] Добавить интеграционную проверку в временной копии репозитория: новый документ с синтетическим ID приводит к ненулевому exit code валидатора, вывод содержит путь/строку, но не ID. Документ с placeholders проходит. Копия не должна включать `.git`, node_modules, персональный ярлык или реальные диагностические данные.
- [x] Запустить новые тесты и `node scripts/validate-repo.mjs`. Результат — PASS. Повторная подстановка синтетического ID в тестовой копии должна снова давать FAIL.

**Результат:** обнаруживаются обе ранее пропущенные формы идентификаторов, без ослабления имеющихся проверок и без утечки совпадений в лог CI.

## Задача 4. Документировать обращение с локальными данными и проверить публикацию

**Files:** `docs/maintenance.md`; существующие `.gitignore` и `.github/workflows/ci.yml` — проверка без обязательной правки.

- [x] Добавить раздел «Данные перед публикацией»: использовать placeholders для ID; не пересылать персональную сборку и identity-файлы; `.tmp-figma` содержит рабочие выгрузки; ZIP всей локальной папки с разыменованием ярлыков отличается от Git-архива tracked файлов.
- [x] Описать repo-local noreply-настройку для новых клонов. Уточнить, что она не меняет старые коммиты и может быть переопределена переменными окружения.
- [x] Проверить `git ls-files .tmp-figma 'Плагин Figma'` — пусто; `git check-ignore .tmp-figma 'Плагин Figma'` — оба пути игнорируются. Исходники персональных файлов не открывать и не менять повторно без необходимости.
- [x] Выполнить из корня репозитория:

```bash
node --test plugins/figma-local-bridge/tests/sensitive-data.test.mjs
node scripts/validate-repo.mjs
npm test --prefix plugins/figma-local-bridge
npm run schema:check --prefix plugins/figma-local-bridge
npm run verify --prefix plugins/figma-local-bridge
git diff --check
```

- [x] Сканировать новый tracked snapshot Gitleaks с `--redact=100 --ignore-gitleaks-allow`; отчёт хранить вне репозитория. Объяснить каждое срабатывание; не добавлять общий allowlist на generic-api-key ради зелёной проверки.
- [x] Передать готовый diff read-only агенту: искать пропущенные формы ID, реальные значения в новых тестах/плане, сломанные ссылки и регрессии обхода файлов. Родитель проверяет каждое замечание по исходникам.
- [ ] Подготовить один коммит текущей очистки и проверок. Проверить author и committer email самого коммита. Перед push показать состав, SHA, ветку, remote и получить подтверждение по правилу публикации.
- [ ] После push повторно прочитать remote SHA и дождаться результатов CI. Старые находки истории отдельно остаются в отчёте.

**Критерий этапа A:** пять текущих вхождений обезличены; регрессии и проектные проверки проходят; новый коммит не раскрывает корпоративный email; локальные макеты и ключи сохранены.

## Задача 5. Отдельное решение об опубликованной истории — этап B

Это отдельное согласование: переписывание изменит SHA старых и последующих коммитов. Этап A сам по себе этого не делает.

**Files:** только приватный изолированный clone/mirror и материалы вне основного checkout; после согласованной публикации — синхронизация рабочих клонов.

- [ ] Объяснить два варианта: оставить документированную историю при отсутствии подтверждённых рабочих секретов либо удалить из неё идентификаторы и корпоративный email. Согласовать объём истории: конкретные ветки/теги и категории замен. Не считать простое одобрение этапа A разрешением на этап B.
- [ ] Перед любыми преобразованиями сохранить приватный Git bundle и карту refs; проверить читаемость резервной копии. Она сама содержит прежние чувствительные данные и не должна оказаться на GitHub.
- [ ] Подготовить свежий изолированный клон. Найти все уникальные Figma/session-ID и авторские email во всех согласованных refs; составить приватную карту замен. Включить реальные значения только в эту временную карту, не в shell history, план или tracked файлы.
- [ ] В клоне применить git-filter-repo: замены идентификаторов в содержимом и commit messages; корпоративный email заменить на проверенный noreply в author и committer. Сохранить имена авторов. Старый тестовый HMAC-литерал отдельно классифицировать по его потребителям; не объявлять рабочим секретом и не менять для сокрытия warning без оснований.
- [ ] Не исполнять старые состояния проекта. Для каждого итогового ref проверить содержимое и метаданные; для новой main выполнить проверки этапа A. Повторить полную Gitleaks-проверку и поиск точных исходных ID/email по всей преобразованной истории с маскированным выводом.
- [ ] Просмотреть GitHub releases, tags, PR refs и архивы, чтобы определить границу реально удаляемых копий. Нельзя обещать удаление чужих клонов, форков и всех кэшей. При необходимости проверить актуальную процедуру GitHub для удаления остаточных sensitive-data references.
- [ ] Показать пользователю конкретную карту старых/новых SHA каждого затронутого ref, результаты проверок, remote и последствия для клонов. Получить явное разрешение на публикацию переписанной истории.
- [ ] Перед отправкой повторно прочитать remote refs. Если они изменились после подготовки, остановить публикацию и пересобрать кандидат. Отправлять только согласованные refs с явными ожидаемыми старыми SHA в `--force-with-lease`; не использовать безусловный `--force` или `push --mirror`.
- [ ] Повторно прочитать refs на GitHub, проверить CI и подготовить инструкции перехода существующих клонов на новую историю без потери локальных изменений. Старые ссылки на коммиты обновлять только в пределах согласованного объёма.

**Критерий этапа B:** в управляемых опубликованных refs нет согласованных ID и корпоративного email; тесты проходят; оставшиеся внешние копии и ограничения явно перечислены.

## Что не входит в текущую очистку

- Удаление локальной диагностики и ротация ключей установки без свидетельств их публикации.
- Массовое переименование проектов/библиотек и удаление демонстрационного экрана.
- Обещание абсолютного отсутствия любых чувствительных данных по одному сканеру.
- Автоматическое переписывание истории, force push или смена публичности репозитория.

## Самопроверка плана

- [x] Каждая подтверждённая категория имеет действие: текущие ID — задача 2, будущие ID — задача 3, будущий email — задача 1/4, старые данные — задача 5, локальные данные — задача 4.
- [x] План не содержит реальных ID, email пользователя или ключей; чувствительные карты остаются вне Git.
- [x] Имя детектора, структура результата, пути модулей и тестовая команда согласованы.
- [x] Разделены обычный push, решение о переписывании истории и подтверждение конкретных переписанных refs.


## Результаты реализации этапа A перед коммитом

- Пять текущих вхождений в четырёх документах заменены на placeholders; размеры, nodeId и остальные диагностические данные сохранены.
- Для этого checkout установлен проверенный GitHub noreply-адрес. Фактические author/committer identities соответствуют ему; глобальная конфигурация не менялась.
- Регрессии детектора: исходный RED — 19 ошибок обнаружения без ошибок импорта; итоговый GREEN — 25/25. Интеграционные тесты проверяют настоящий процесс валидатора, безопасный вывод, исключённые каталоги и symlink.
- Независимое ревью выявило пропуск Figma URL в Markdown. Он воспроизведён тестами и исправлен; добавлены проверки autolink, пунктуации и недопустимого продолжения ID. После повторного ревью замечаний не осталось.
- Полный набор тестов — 370/370; схема, MCP-контракт, валидатор и `git diff --check` проходят. Проверены относительные ссылки в изменённых документах. Новый CI ещё не запускался; соединение с Figma не проверялось.
- Gitleaks: исходное дерево — 0; кандидат публикации из 140 файлов — 0. Четыре исходных уникальных ID отсутствуют в кандидате. Приватные карты замен и отчёты находятся вне репозитория.
- Полная доступная история сохраняет 9 прежних срабатываний. Этап B, публикация и удаление локальных данных не выполнялись.

Следующие действия: один локальный коммит с проверкой его email; затем показать SHA, состав, ветку и remote и дождаться подтверждения push. Переписывание истории требует отдельного решения.
