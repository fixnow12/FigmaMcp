---
name: figma-local
description: "Работа с открытым файлом Figma Desktop через локальный MCP: чтение выделения, точечные правки узлов, создание экранов и вставка компонентов. Использовать для запросов на просмотр, создание или изменение макета Figma через Desktop Bridge."
---

# Figma Local

Используй только инструменты MCP-сервера `figma-local`: `get_status`, `get_operation_status`, `inspect_selection`, `export_assets`, `patch_nodes`, `render_screen`, `recreate_screen`, `use_component`, `clone_nodes`, `move_nodes`, `find_assets`, `resolve_resource_keys`, `import_variables`, `resolve_variables`, `bind_variables`, `set_text_links`, `set_reactions`, `activate_page`, `get_file_metadata`, `set_file_metadata`, `get_page_settings`, `set_page_settings`, `set_node_variable_modes`, `capture_library_template` и `assemble_library_template`.

## Общие правила

- Для холста используй только типизированный `figma-local`. Не открывай Figma в браузере и не используй мышь/клавиатуру, Playwright, REST, официальный удалённый MCP, API-токены или произвольный JavaScript. Ссылка нужна для извлечения `fileKey`.
- В начале подключения проверь `get_status` с известным `fileKey`: нужны готовность именно цели и `diagnostics.ready:true`. Если готовность уже подтверждена в текущей работе, переходи к задаче. При нескольких файлах выбирай по запросу пользователя, при неоднозначности уточни цель; не перебирай выделения. Pairing-код не включай в отчёты.
- Не меняй библиотечные файлы и оригиналы компонентов без явного запроса. Перед таким изменением предупреди о влиянии на библиотеку и дождись подтверждения; до этого не передавай `allowComponentChanges:true`. Рабочие фреймы и обычные экземпляры этого флага не требуют.
- Читай справки ниже по условиям соответствующего сценария, до его выполнения. Для обычной текстовой правки достаточно общих правил, рабочего процесса, глубины чтения и обработки результатов. Уже прочитанную актуальную справку повторно загружать не нужно.

## Рабочий процесс

1. Для незнакомого макета сначала вызови `inspect_selection`.
2. Известный узел читай через `nodeId`, несколько узлов — через `nodeIds`. Узел по имени ищи через `find_assets` с `kind: "nodes"`; при неоднозначности уточни цель. Если нет ни ID, ни описания цели, попроси выделить нужный узел в Figma Desktop.
3. Известные правки одного файла с общей целью и общим откатом объединяй в один `patch_nodes`. Если следующая правка зависит от результата предыдущей, выполни её отдельным шагом. Для точечной правки используй `patch_nodes` и стабильные `key` или ID из чтения; не пересоздавай весь экран. Ключ спецификации передавай как `{key:"screen-title",set:{…}}`, Figma ID — как `{id:"16:85",set:{…}}`: эти поля не взаимозаменяемы. Для сравнения оформления читай `detail: "full"`: он возвращает типографику, стили, привязки переменных и настройки размеров.
4. Для варианта существующего экрана (добавить промо, ошибку, состояние, изменить блок) используй `recreate_screen` с `sourceId` и `changes`, даже если пользователь говорит «нарисуй варианты», а не «воссоздай». Для копии без изменения внутренней структуры — `clone_nodes`, для переноса — `move_nodes`. `render_screen` используй для дизайна с нуля, когда нет исходного экрана или пользователь запросил новую композицию. Все узлы должны иметь уникальные стабильные `key`.
5. Экземпляры компонентов создавай через `use_component`: локальный источник задаётся `sourceId` или `sourceKey`, библиотечный — `libraryKey`; родитель — `parentId` или `parentKey`.
6. Для проверки итоговых свойств вместе с PNG используй один `inspect_selection` с `detail:"full"` и `screenshot:true`, адресованный корню результата. После последней записи, меняющей геометрию, проверь свежий PNG результата. Для `patch_nodes` передай `screenshotKey` корневого экрана либо сразу после патча вызови `inspect_selection` с `screenshot:true`: снимок, полученный до append/set, не проверяет итог. Для варианта сравни его с исходником при одинаковом масштабе: ширина экрана, оболочка, навигация, иконки, неизменённые блоки и типографика должны сохраняться. Успешная запись, Auto Layout и отсутствие выходов за границы не доказывают сходство или правильность сценария.

Для первой новой спецификации используй `render_screen` с `dryRun:true`: он проверяет свойства, координаты и загрузку шрифтов без создания слоёв. Это не проверка итогового SVG, кликов или визуального совпадения. Координаты `x/y` требуют у непосредственного родителя `layout.direction:"none"` либо у дочернего слоя `layoutPositioning:"ABSOLUTE"`; это правило проверяется и для вложенных `parentKey`. Выбирай `ABSOLUTE` для отдельного overlay; не меняй весь контейнер на `direction:"none"`, если это сломает раскладку соседей. После первого тайм-аута Bridge сам один раз сверяет точное семейство/начертание с каталогом Figma и повторяет только ту же загрузку. Если возвращён `FONT_SERVICE_TIMEOUT` с `retryPolicy:"after_state_change"`, неизменённый `sleep + retry` запрещён. Сам агент не создаёт временный font-probe и не управляет вкладкой вне MCP: попроси пользователя вывести действительно фоновую вкладку на передний план или вручную применить именно этот шрифт к временному тексту. После подтверждения повтори один `dryRun` с тем же проверенным JSON. `activate_page` переключает страницу, а не вкладку, и не считается восстановлением font service. Не проси открыть уже открытую вкладку. Наличие WebSocket и ответ Plugin API не гарантируют загрузку шрифтов; `isActive` сам по себе не доказывает причину. Тайм-аут не доказывает отсутствие шрифта и не разрешает его подмену.

ID узла принадлежит конкретному файлу: всегда сохраняй пару `(fileKey,nodeId)`. `sourceId` в `clone_nodes`/`recreate_screen` должен принадлежать переданному `fileKey`; команды не переносят узлы между файлами. Для нового результата в другом файле прочитай источник с его `fileKey`, затем используй новую спецификацию `render_screen` в файле назначения. Ошибка сети при чтении чужого ID не доказывает новую возможность или её отсутствие — сначала исправь адресацию.

При работе от исходника сохраняй его шаги и платформенные размеры; не заменяй телефон произвольной широкой карточкой и не удаляй оболочку ради новой секции. Новый блок оформляй по ближайшему аналогу в исходнике. Если запрошены web/app, а доступен только app, найди web-исходник через `find_assets`; при его отсутствии обозначь web как новую концепцию, не как точную адаптацию. Числа лимитов и даты используй из подтверждённых условий либо явно помечай как пример; дневной и месячный лимиты не взаимозаменяемы. URL в подписи макета не означает, что прототипный переход настроен.

## Глубина чтения

- В `inspect_selection` передавай целые числа: `depth` от 0 до 8 (по умолчанию 3), `maxNodes` от 1 до 1000 (по умолчанию 200). `depth` считается от каждого запрошенного узла; 0 возвращает сам узел без детей. Для `nodeIds` допустимо от 1 до 50 ID; не передавай одновременно `nodeId` и `nodeIds`.
- Для более глубокого дерева читай ветки отдельными вызовами: при `coverage.complete: false` бери `nodeId` из `coverage.unread` и вызывай `inspect_selection` с этим ID, тем же `fileKey`, `detail: "full"` и `depth: 8`. Повторяй для оставшихся непрочитанных веток. Не увеличивай `depth` выше 8 или `maxNodes` выше 1000. Пока остаются непрочитанные релевантные ветки, нельзя помечать полную проверку текстов, шрифтов или геометрии как пройденную; каждую исключённую ветку перечисли в отчёте с проверяемой причиной нерелевантности.
- Если чтение отклонено с `Input validation error` (может отображаться как `MCP error -32602`), исправь указанное в сообщении поле перед повтором. Например, для `Number must be less than or equal to 8 at depth` уменьши `depth` до 8 и дочитай ветки отдельно; повтор того же запроса не поможет.
- Для обзора большой страницы начни с `depth:1` и `maxNodes:100`, затем читай нужные ветки по ID. Если ответ обрезан клиентом, обрабатывай сохранённый JSON локально; увеличение глубины увеличивает ответ. При `FIGMA_READ_TIMEOUT` отсутствие узла или библиотеки не подтверждено. Ошибка содержит `blockers` с зависшим ресурсом; проверь статус этого `fileKey`, затем выбери меньшую ветку или другой подтверждённый источник. Составные ID экземпляров возвращай целиком, как в чтении.

## Точное воссоздание

Перед воссозданием экрана, созданием варианта от исходника или переносом его оформления прочитай [порядок воссоздания](references/reconstruction-workflow.md). Там описаны чтение исходника, сохранение свойств, ограничения и проверка; параметры changes — в связанной справке.

## Дизайн-система и готовые блоки

Перед поиском компонентов, стилей или Variables, вставкой экземпляра, клонированием, переносом или привязкой переменных прочитай [работу с готовыми ресурсами](references/assets-workflow.md).

## Ссылки и прототипы

Перед чтением, созданием или изменением текстовых ссылок и прототипных переходов прочитай [порядок работы со связями](references/links-workflow.md). PNG не подтверждает кликабельность; без настоящих кликов в Present укажи `actualClicks:"not-tested"`.

## Подключение

При неготовом статусе, отсутствии нужного инструмента или поля в каталоге, ошибках связи/версий либо клиентского разбора JSON сначала прочитай [диагностику подключения](references/connection.md). Там же — типизированный CLI для устаревшего каталога. При READY дополнительная диагностика не нужна.

## Контекст файла

Перед `capture_library_template`, `assemble_library_template`, `activate_page`, `get_file_metadata` или `set_file_metadata` прочитай [шаблоны, страницы и метаданные файла](references/file-context.md). Это также маршрут для запросов на сборку из библиотечного шаблона, переключение страницы, имя файла и thumbnail.

## Результат и ошибки

- `operationStatus: "applied"` означает, что изменения выполнены. При `screenshot.status: "failed"` не повторяй запись: получи снимок отдельным `inspect_selection` по ID из результата.
- `render_screen` создаёт корневой FRAME внутри служебной SECTION. Сначала проверь согласованность `result.placement`: `rootParentId === sectionId`, `sectionParentId === destinationParentId`, `wrapperType === "SECTION"`; `destinationParentId` должен быть ожидаемой PAGE. Затем независимо прочитай `rootId` и `sectionId` через `inspect_selection` с `detail:"full"`: фактические `parentId` должны образовать ту же цепочку. Без read-back формулируй только «размещение заявлено результатом записи», не «родитель проверен». Не сравнивай `root.parent` напрямую с PAGE.
- После compaction или восстановления сессии сначала сопоставь сохранённый результат по call ID, `fileKey`, target key/ID и `operationStatus`, затем прочитай фактические узлы. Резюме сессии не является доказательством, что запись не выполнялась; уже применённый append нельзя повторять без read-back ключей или ID. Если запись была `applied`, но read-back сейчас недоступен, сообщи «запись применена, проверка не выполнена» и оставь итог черновиком, а не `passed`.
- `not_applied` — предварительная проверка отклонила пакет; `rolled_back` — изменения пакета отменены; `partial` — восстановление неполное, проверь указанные узлы и `rollbackErrors`.
- После `partial` прочитай затронутые узлы до следующей записи. Если требуемое значение уже сохранено, повторять его не нужно. Одинаковая ошибка на каждом узле пакета требует диагностики Bridge, а не серии одиночных записей с объявлением этих ошибок штатным успехом.
- `unknown` после тайм-аута или обрыва связи не доказывает, что изменений не было. Не повторяй изменяющую команду автоматически; сначала дождись восстановления соединения и прочитай затронутые узлы. Тайм-аут не может прервать уже выполняющийся вызов Plugin API.
- После timeout `set_file_metadata` не повторяй запись: дождись освобождения файла и вызови `get_file_metadata` с тем же `fileKey`, чтобы прочитать фактические имя, thumbnail и список страниц.
- При `PLUGIN_UNRESPONSIVE` новая команда не отправлена: попроси открыть целевую вкладку Figma и проверить окно Bridge. При `PLUGIN_BUSY` / `FILE_BUSY` предыдущая команда ещё выполняется. Следуй `nextStep`; не устраивай циклы `sleep 20/60/90` с одинаковыми запросами. После тайм-аута предыдущей записи её результат по-прежнему неизвестен, даже если следующая команда имеет `not_applied`.
- Замена содержания текста со смешанным оформлением требует явных `textRuns` для нового текста; не заменяй смешанные шрифты/цвета одним значением без запроса пользователя.

Формат экрана, токены и правила стабильных ключей описаны в [references/design-spec.md](references/design-spec.md). Для `render_screen` используй плоский `spec.nodes` и `parentKey`; каждую новую или изменённую `spec` для `render_screen` сохрани и проверь через `scripts/validate-spec.mjs` из корня плагина перед dryRun. Исправь или явно проверь каждый `geometryWarnings`: предупреждение не блокирует валидность спеки, но запрещает считать геометрию проверенной. Отправляй тот же проверенный JSON без ручной пересборки. Успех dryRun относится только к переданному пакету: шрифты последующих слайдов и полное чтение результата он не подтверждает. Если клиент не передал PNG модели либо модель не может его просмотреть, отметь визуальную проверку как `not-tested` и оставь результат черновиком.

### Возможности guide-fidelity v2

Новые lists/indentation, GLASS, свойства векторов и диапазонные переменные включайте только по свежему адресному `get_status`: нужны `diagnostics.ready`, `current.mcp/broker/plugin`, совпавший `capabilityFileKey` и нужная строка `diagnostics.capabilities`. Версия manifest сама по себе недостаточна. Полный контракт — раздел «Проверка точности guide-fidelity v2» в README.

`bind_variables` принимает парные UTF-16 `start/end`, `fontFamily`, числовой `fontWeight`, `fontSize`, `lineHeight` и `fills`/`paintIndex`. Полный inspect возвращает `textSegments` и aliases. Внутри INSTANCE `vectorPaths`, `booleanOperation`, `pointCount` допускаются только при точном совпадении с текущим значением; реальные изменения блокируются без detach. GLASS `splay` сохранён из реального чтения, но требует отдельного сравнения результата. NAVIGATE сравнивает подтверждённые defaults (`resetScrollPosition:true`, `resetVideoPosition:false`), сохраняя явные различия. Клики в Present этим не проверены.

Нативный масштаб INSTANCE сохраняйте отдельным `patch_nodes` с абсолютным `scaleFactor` по capability `instance-scale-v1`. Bridge выполняет rescale до resize и проверяет отношение 0.01–100; изменение вложенного INSTANCE запрещено. Сначала задайте масштаб корневого экземпляра, затем отдельным пакетом проверьте/примените потомков. `inspect_selection(detail:"full")` возвращает scaleFactor.

`set_node_variable_modes` задаёт режим коллекции на точных FRAME/INSTANCE: `{fileKey, bindings:[{nodeId,collectionKey,anchorVariableKey,modeName}]}` (до 40). Получи ключи из исходного чтения/каталога и работай по capability `node-variable-modes-v1`. Ответ `variableModesVerification.checks` подтверждает collectionId/modeId и truthful `mutated`. Повторное чтение `inspect_selection` проверяет `explicitVariableModes`; значения переменных и определения компонентов операция не меняет.

### Журнал импорта переменных

При capability `operation-journal-v1` перед импортом сохраняй UUID `operationId` и номер `attempt` вместе с исходными аргументами. После timeout не повторяй import_variables. `get_operation_status({fileKey,operationId})` читает сохранённый журнал без очереди холста; проверяй точный fileKey, argsHash, attempt и завершение операции. Поздний ID требует свежего resolve_variables перед признанием ресурса готовым. Пока native-операция не завершена, запись остаётся заблокированной. Журнал сохраняет события после исчезновения MCP-клиента и после безопасного переподключения того же плагина. Старые unknown без журнала не считаются восстановленными. В конструкторе гайдов весь этот маршрут выполняет guide.mjs recover/resume; не создавай циклы ожидания в чате.
